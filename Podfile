use_frameworks!

target 'TextSearch' do
  # pod 'AMPopTip' # gw: bad quality. causing CPU +22%
  pod 'FaceCropper', :path => '~/workspace/FaceCropper'
  pod 'Google-Mobile-Ads-SDK', '~>7.37.0'
  pod 'GoogleSignIn'
end
